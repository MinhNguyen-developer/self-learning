var course = {
  name: "Javascript",
  coin: 250,
};

// if (course.coin > 0) {
//   console.log(`${course.coin} Coins`);
// } else {
//   console.log("Free");
// }

const result = course.coin > 0 ? `${course.coin} Coins` : "Free";

var a = 1;
var b = 2;

var c = a > 0 ? a : b;
